# WORDPRESS REACT BOILER PLATE

This is the wordpress template file to use react as a full website. Where we build a react app on the home url in wordpress using tailwind for styling and using vite to bundle for wordpress.